<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zerock.mapper.GoodsMapper">

	<!-- 	메서드명		 반환타입 -->
	<select id="getList" resultType="org.zerock.domain.GoodsVO"> 
		<![CDATA[
		select * from Shop_goods where goods_seq > 0
		]]>
	</select>
	
	<insert id="insert">
	insert into Shop_goods (goods_seq, gname, price, quantity, information, title_img, info_img) values (goods_seq.nextval, #{gname}, #{price}, #{quantity}, #{information}, #{title_img}, #{info_img})
	</insert>
	
	<insert id="insertSelectKey">
	<!-- key인 goods_seq를 ? 전에 찾아서.. -->
	<selectKey keyProperty="goods_seq" order="BEFORE" resultType = "long">
		select goods_seq.nextval from dual
	</selectKey>	
	
	insert into Shop_goods (goods_seq, gname, price, quantity) values (#{goods_seq}, #{gname}, #{price}, #{quantity});
	</insert>
	
	<select id="read" resultType="org.zerock.domain.GoodsVO">
	select * from Shop_goods where goods_seq = #{goods_seq}
	</select>
	
</mapper>